# Docker Compose for local development
# Provides Redis for Celery broker and Fuseki for RDF graph storage

version: '3.8'

services:
  redis:
    image: redis:7-alpine
    container_name: synaptiq-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Apache Fuseki - SPARQL server with TDB2 backend for RDF graph storage
  fuseki:
    image: stain/jena-fuseki:latest
    container_name: synaptiq-fuseki
    ports:
      - "3030:3030"
    volumes:
      - fuseki_data:/fuseki
    environment:
      - ADMIN_PASSWORD=${FUSEKI_ADMIN_PASSWORD:-admin123}
      - JVM_ARGS=-Xmx2g
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3030/$/ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    restart: unless-stopped

  # Optional: Local MongoDB for development
  # Uncomment if you want to run MongoDB locally instead of Atlas
  # mongodb:
  #   image: mongo:7
  #   container_name: synaptiq-mongodb
  #   ports:
  #     - "27017:27017"
  #   volumes:
  #     - mongodb_data:/data/db
  #   environment:
  #     MONGO_INITDB_DATABASE: synaptiq
  #   healthcheck:
  #     test: echo 'db.runCommand("ping").ok' | mongosh localhost:27017/test --quiet
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5

  # PostgreSQL - Agent session persistence
  postgres:
    image: postgres:16-alpine
    container_name: synaptiq-postgres
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=synaptiq_agents
      - POSTGRES_USER=synaptiq
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-synaptiq123}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U synaptiq -d synaptiq_agents"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

volumes:
  redis_data:
  fuseki_data:
  postgres_data:
  # mongodb_data:


